
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ===== HELPER FUNCTIONS =====
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function getUserData(userId) {
      return get(/databases/$(database)/documents/users/$(userId)).data;
    }

    function isAdmin() {
      return isSignedIn() && getUserData(request.auth.uid).role == 'admin';
    }

    function isNotDeleted(userId) {
      // Deny if status is 'deleted'. Allow if field is missing or different.
      return getUserData(userId).status != 'deleted';
    }

    // ===== USERS =====
    match /users/{userId} {
      // A user can read their own profile. Admins can read any profile.
      allow get: if (isOwner(userId) || isAdmin()) && isNotDeleted(userId);
      
      // A new user can create their own profile.
      // The 'role' must be 'user' and they cannot set a 'status'.
      allow create: if isOwner(userId) 
                    && request.resource.data.role == 'user'
                    && !('status' in request.resource.data);

      // A user can update their own profile, but not their role or status.
      // Admins can update any user's profile (e.g., to change a role).
      allow update: if (isOwner(userId) 
                      && request.resource.data.role == resource.data.role
                      && request.resource.data.status == resource.data.status)
                    || isAdmin();
      
      allow list: if isAdmin(); // Only admins can list users.
      allow delete: if false; // Disallow client-side deletes. Use soft-delete pattern.
    }

    // ===== BLOG POSTS =====
    match /blog_posts/{blogPostId} {
      allow get, list: if resource.data.isPublished == true;
      allow read: if isAdmin();
      allow create, update, delete: if isAdmin();
    }
    
    // ===== LEARNING (STATIC CONTENT) =====
    match /learning_modules/{moduleId} {
      allow get, list: if true;
      allow write: if isAdmin();
      match /topics/{topicId} {
        allow get, list: if true;
        allow write: if isAdmin();
      }
    }

    // ===== USER-SPECIFIC DATA =====
    match /users/{userId}/{subcollection}/{docId} {
       // Users can manage their own sub-collection data (progress, etc.)
       allow read, write: if isOwner(userId) && isNotDeleted(userId);
    }
    
    // ===== AUDIT & RISK LOGS (Admin-focused) =====
    // Admins can read any audit/risk logs. Users can't read them directly.
    match /audit/{docId} {
      allow read, write: if isAdmin();
    }

    match /risk/{userId} {
       allow read, write: if isAdmin();
    }

    // ===== TOOL USAGE LOGS =====
    match /tool_usage_logs/{logId} {
        allow read, update, delete: if false;
        allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
    }
  }
}
