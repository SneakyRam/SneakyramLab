// This file is no longer needed as the logic is now handled by the group layout at /src/app/(protected)/layout.tsx
// Keeping a blank file or removing it is fine. For this change, we'll effectively remove it by blanking it out.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
